# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=
# The single Telegram user ID allowed to use this bot
TELEGRAM_ALLOWED_USER_ID=
# Optional GitHub token for headless `gh` CLI and HTTPS git operations.
# Prefer a fine-grained PAT with only the repository permissions the bot needs.
GH_TOKEN=
